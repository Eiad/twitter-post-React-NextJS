import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FF9A8B', 
    },
    secondary: {
      main: '#FF6A88', 
    },
    background: {
      default: '#FFF6F6', 
    },
    text: {
      primary: '#4A4A4A',
    },
  },
});

export default theme;

